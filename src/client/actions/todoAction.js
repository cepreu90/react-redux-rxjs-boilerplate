export const ADD = 'ADD'
export const SUB = 'SUB'

// add action used in the component to dispatch
export const add = () => ({
    type: ADD,
})

// sub action used in the component to dispatch
export const sub = () => ({
    type: SUB,
})
